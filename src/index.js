import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

/**
 * Renders the React application and mounts it to the root element of the HTML document.
 * @param {JSX.Element} component - The root component of the React application.
 * @param {HTMLElement} rootElement - The root element in the HTML document to mount the application.
 */
ReactDOM.render(<App/>, document.getElementById("root"));

